# 🎨 Этап рендеринга (Rendering Pipeline)

Когда браузер получил HTML, CSS и JS — начинается **визуализация страницы**.  
Это последовательность шагов:

---

## 1. 🧩 Парсинг и построение дерева

- **HTML → DOM (Document Object Model)**  
    Каждому тегу соответствует узел дерева.
- **CSS → CSSOM (CSS Object Model)**  
    Из всех CSS-файлов и `<style>` собирается структура стилей.
- **DOM + CSSOM → Render Tree**  
    Строится дерево только видимых элементов (например, `display: none` не попадает).

> 🧠 _Render Tree содержит узлы с вычисленными стилями (цвет, размер, позиция)._

---

## 2. 📏 Layout (или Reflow)

- На основе Render Tree браузер **вычисляет геометрию элементов**:
    - координаты,
    - размеры,
    - расположение относительно других элементов.
- Это стадия **вычисления layout-а страницы** — фактический "чертёж".

⚠️ Изменение размеров или позиционирования элементов (через JS/CSS) может вызвать **reflow**,  
что дорого по производительности.

---

## 3. 🖌️ Paint (отрисовка)

- После расчёта позиций браузер **рисует пиксели**
    - фон, текст, тени, границы, изображения и т.д.
- Каждый визуальный элемент превращается в растровые команды.

> 🎨 _Результат — множество «слоёв» (layers)._

---

## 4. 🧮 Compositing (композитинг)

- Если элементы находятся на разных слоях (например, `transform`, `opacity`, `position: fixed`),  
    браузер **собирает** их в единый кадр.
- Для ускорения используется **GPU**.

⚡ Это позволяет обновлять отдельные слои без пересчёта layout — например, для анимаций.

---

## 5. 🖥️ Display (визуализация)

- Финальный кадр отображается на экране.
- Для динамических страниц процесс может частично повторяться:
    - JS изменил DOM → возможен reflow / repaint / composite.

---

## 📋 Итоговая таблица

|Этап|Что делает|Что создаётся|
|---|---|---|
|**DOM**|Парсинг HTML|DOM дерево|
|**CSSOM**|Парсинг CSS|CSS дерево|
|**Render Tree**|Объединение DOM и CSSOM|Визуальные узлы|
|**Layout**|Расчёт геометрии|Позиции и размеры|
|**Paint**|Рисовка элементов|Пиксели|
|**Composite**|Сборка слоёв|Финальный кадр|

---

## 💡 Кратко для собеседования

> Рендеринг — это конвейер: браузер строит DOM и CSSOM, объединяет их в Render Tree,  
> вычисляет layout, рисует (paint) и собирает слои (composite).  
> Любые изменения DOM или стилей могут вызвать reflow или repaint.