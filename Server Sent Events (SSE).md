**Server-Sent Events** — это технология для односторонней передачи данных от сервера к клиенту в реальном времени через HTTP.

## Сравнение трёх подходов

### 🔄 Классический HTTP (Request-Response)

**Как работает:**

- Клиент отправляет запрос → сервер отвечает → соединение закрывается
- Для получения новых данных нужен новый запрос

**Особенности:**

- ✅ Простота, универсальность
- ✅ Кэширование, совместимость с любой инфраструктурой
- ❌ Нет реального времени без polling/long-polling
- ❌ Overhead на каждый запрос (заголовки, установка соединения)

**Когда использовать:** Обычные CRUD операции, где данные запрашиваются по требованию

---

### 📡 Server-Sent Events (SSE)

**Как работает:**

- Клиент отправляет один HTTP-запрос → сервер держит соединение открытым
- Сервер отправляет данные когда они появляются (односторонний поток)
- Использует `Content-Type: text/event-stream`

**Особенности:**

- ✅ Простая реализация (встроенный EventSource API в браузере)
- ✅ Автоматическое переподключение
- ✅ Поддержка ID событий для восстановления после разрыва
- ✅ Работает через обычный HTTP/HTTPS (проходит через прокси, файрволы)
- ❌ Только UTF-8 текст (нет бинарных данных)
- ❌ Только от сервера к клиенту (нет bidirectional)
- ❌ Ограничение на количество соединений к одному домену (~6 в браузере)

**Пример клиента:**

```javascript
const eventSource = new EventSource('/api/events');

eventSource.onmessage = (event) => {
  console.log('Новые данные:', event.data);
};

eventSource.addEventListener('price-update', (event) => {
  console.log('Цена:', JSON.parse(event.data));
});
```

**Пример сервера (Node.js):**

```javascript
res.writeHead(200, {
  'Content-Type': 'text/event-stream',
  'Cache-Control': 'no-cache',
  'Connection': 'keep-alive'
});

// Отправка данных
res.write(`data: ${JSON.stringify({price: 100})}\n\n`);

// С именем события и ID
res.write(`event: price-update\nid: 123\ndata: {"price": 105}\n\n`);
```

**Когда использовать:**
- Новостные ленты, уведомления
- Обновления данных в реальном времени (котировки, статусы)
- Прогресс выполнения задач
- Логи и мониторинг

---

### 🔌 WebSocket (WS)

**Как работает:**

- Начинается с HTTP Upgrade запроса
- Устанавливается постоянное двустороннее соединение
- Клиент и сервер могут отправлять сообщения в любой момент

**Особенности:**

- ✅ Полностью двусторонняя связь (full-duplex)
- ✅ Поддержка бинарных данных
- ✅ Минимальный overhead (нет HTTP заголовков после установки)
- ✅ Нет лимитов на количество соединений
- ❌ Сложнее в реализации (нужна библиотека, обработка переподключений)
- ❌ Может блокироваться корпоративными файрволами
- ❌ Нужно самому реализовывать ping/pong, переподключение

**Пример клиента:**

```javascript
const ws = new WebSocket('wss://example.com/socket');

ws.onmessage = (event) => {
  console.log('Получено:', event.data);
};

ws.send('Hello from client'); // Отправка на сервер
```

**Когда использовать:**

- Чаты, мессенджеры
- Онлайн-игры
- Коллаборативное редактирование
- Любое двустороннее взаимодействие в реальном времени

---

## 📊 Быстрое сравнение

|Характеристика|HTTP|SSE|WebSocket|
|---|---|---|---|
|Направление|Request-Response|Сервер → Клиент|Двустороннее|
|Протокол|HTTP|HTTP|WS (поверх HTTP)|
|Формат данных|Любой|Текст (UTF-8)|Текст + бинарные|
|Сложность|Простой|Простой|Средняя|
|Переподключение|Вручную|Автоматическое|Вручную|
|Совместимость|Отличная|Хорошая|Хорошая|

## 💡 Резюме

Выбирай **SSE**, если тебе нужны:
- Обновления от сервера к клиенту (уведомления, feed'ы)
- Простота реализации
- Стандартная HTTP инфраструктура

Выбирай **WebSocket**, если нужны:
- Двусторонняя коммуникация (чат, игры)
- Высокая частота сообщений
- Бинарные данные

Оставайся с **HTTP**, если:
- Данные запрашиваются редко и по требованию
- Нужно кэширование
- Нет требований реального времени