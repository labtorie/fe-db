### 🧭 1. Ввод URL и подготовка запроса

- Пользователь вводит адрес (например, `https://example.com`).
- Браузер определяет протокол (HTTP/HTTPS), хост, путь, параметры.
- Проверяет кеш (браузера, Service Worker, DNS, OS) — вдруг страница уже есть локально.

---

### 🌐 2. DNS-резолвинг

- Преобразует доменное имя (`example.com`) в IP-адрес.
- Последовательно проверяются:
    - локальный DNS-кеш браузера/системы,
    - файл `hosts`,
    - DNS-сервер провайдера,
    - корневые / TLD / авторитетные DNS-серверы.
- Результат кешируется.

---

### 🔗 3. Установление соединения

- Определяется IP и порт (обычно 80 или 443).
- Устанавливается TCP-соединение (3-way handshake).
- Если HTTPS:
    - выполняется **TLS-рукопожатие** — шифрование, сертификаты, обмен ключами.

---

### 📤 4. Отправка HTTP-запроса

- Браузер формирует запрос:
   ```
   GET / HTTP/1.1
   Host: example.com
   User-Agent: Chrome/...
   ```
- Передаёт его по установленному соединению.

---

### 📥 5. Обработка на сервере

- Сервер (например, Nginx → Node.js/Express) получает запрос.
- Обрабатывает логику, может обратиться к БД, API и т.д.
- Возвращает **HTTP-ответ** (статический HTML или JSON и т.д.).

---

### 📦 6. Получение ответа браузером

- Ответ содержит:
    - **Статус** (200, 301, 404 и т.п.)
    - **Заголовки**
    - **Тело** (HTML, JSON, файл)
- Возможны редиректы → новый запрос.

---

### 🧩 7. Парсинг и построение дерева

- HTML начинает парситься **потоково**:
    - создаётся **DOM** (Document Object Model),
    - при встрече `<link>` или `<script>` → делаются дополнительные сетевые запросы.
- CSS-файлы парсятся в **CSSOM** (CSS Object Model).

> Для `<link>` можем передать атрибут `rel = preload, prefetch`. 

#### 🚀 `<link rel="preload">`

### 🔹 Что делает

— Позволяет **заранее загрузить** критически важные ресурсы (скрипты, стили, шрифты, изображения),  
чтобы они были готовы к моменту, когда браузеру они реально понадобятся.

`<link rel="preload" href="/styles/main.css" as="style"> <link rel="preload" href="/script/app.js" as="script"> <link rel="preload" href="/fonts/Roboto.woff2" as="font" type="font/woff2" crossorigin>`

### 🔹 Как работает

- Ресурс **загружается сразу** (высокий приоритет).
- Когда он встретится в HTML (например, в `<script>`), он **уже будет в кеше**.
- Используется для **критически важных ресурсов**, влияющих на первый рендер.

### 🔹 Пример

> Подходит для CSS/JS, без которых нельзя показать первый экран (Critical Rendering Path).

### ⚠️ Важно

- Обязательно указывать `as="..."` — иначе браузер может загрузить ресурс повторно.
- Не злоупотреблять: слишком много `preload` → блокирует важные ресурсы.
#### 🕓 `<link rel="prefetch">`

### 🔹 Что делает

— Сообщает браузеру, что **ресурс может понадобиться в будущем**,  
и его можно загрузить **на фоне с низким приоритетом**.

`<link rel="prefetch" href="/next-page.html"> <link rel="prefetch" href="/assets/next-page.js" as="script">`

### 🔹 Как работает

- Ресурс загружается **в фоновом режиме**, без влияния на текущий рендер.
- Сохраняется в кеш.
- При переходе на следующую страницу загрузка будет мгновенной.

### 🔹 Пример

> Отлично подходит для **SPA-навигации**, **lazy routes**, **следующих страниц**.

### ⚠️ Важно

- Работает **только при низкой загрузке сети/CPU**.
- Не гарантирует загрузку (браузер может проигнорировать).
---

# ⚙️ `async` vs `defer`

|Атрибут|Когда загружается|Когда выполняется|Порядок выполнения|Типичный сценарий|
|---|---|---|---|---|
|**`async`**|Параллельно с парсингом HTML|Сразу после загрузки (останавливает парсинг)|**Не гарантируется** (по мере загрузки)|Независимые скрипты — аналитика, реклама|
|**`defer`**|Параллельно с парсингом HTML|После полной загрузки HTML, **перед DOMContentLoaded**|**Сохраняется порядок**|Основные скрипты приложения|

---

### 💡 Коротко:

- `async` — **загружается и выполняется как можно раньше**, без ожидания других.
- `defer` — **загружается параллельно**, но **выполняется после парсинга DOM**.

---

📘 Пример:

`<script src="analytics.js" async></script> <!-- независимый --> <script src="main.js" defer></script>     <!-- основной -->`

> 🧠 На собеседовании:  
> “`async` — для независимых скриптов, `defer` — для тех, что работают с DOM после его загрузки.”
----
### 🎨 8. [[Рендеринг]]

- DOM + CSSOM → объединяются в **Render Tree**.
- Этапы:
    1. **Layout (Reflow)** — рассчитываются размеры и позиции элементов.
    2. **Paint** — рисуются пиксели (цвета, тени и т.п.).
    3. **Composite** — объединяются слои, формируется итоговое изображение.


---

### ⚡ 9. Выполнение JavaScript

- JS-движок (например, V8) выполняет скрипты.
- Может модифицировать DOM → вызывает **reflow / repaint**.
- После загрузки и выполнения JS запускается, например, React/Vue/Angular-логика.

---

### 🔁 10. Отображение страницы и последующие события

- Пользователь видит страницу.
- Загружаются дополнительные ресурсы (шрифты, изображения, API-данные).
- При действиях пользователя происходят новые JS-вызовы, запросы (XHR / fetch) и обновления DOM.

---

📘 **Короткий вариант для устного ответа:**

> После ввода URL браузер резолвит DNS → устанавливает TCP/TLS соединение → отправляет HTTP-запрос → получает ответ → парсит HTML, CSS, JS → строит DOM, CSSOM, Render Tree → выполняет JS → отрисовывает страницу.
